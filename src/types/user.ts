export type User = {
  userId: number;
  name: string;
  email: string;
  profileImageUrl: string;
  company: string;
  position: string;
  isActive: boolean;
  createdAt: string;
  updatedAt: string;
};
