export type User = {
  userId: number;
  name: string;
  email: string;
  profileImageUrl: string;
  company: string;
  rank: string;
  isActive: boolean;
  createdAt: string;
  updatedAt: string;
};
